
/**
 * This service is deprecated as the app logic has changed to 
 * "Reporting to Mom" using local scripts instead of "Mom replying via AI".
 */

export const generateMomResponse = async (): Promise<string> => {
  return "";
};

export const generateDateIdea = async (): Promise<string> => {
  return "去公园散步";
};
